import { ImcHighlightDirective } from './imc-highlight.directive';

describe('ImcHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImcHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
